services:
  - type: web
    name: cvconnect
    env: node
    nodeVersion: 18.17.0
    buildCommand: npm install && npm run build
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: JWT_SECRET
        generateValue: true
    healthCheckPath: / 